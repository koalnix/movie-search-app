export default {
  // The base URL for API requests
  apiBaseUrl: "/api",

  // Styling configuration for the app
  styling: {
    // Sizing settings, specifically for containers
    sizing: {
      container: {
        // Maximum width for the container
        maxWidth: "1024px",
      },
    },

    // Color configuration for different parts of the app
    color: {
      // Primary and secondary colors for key elements (e.g., buttons, highlights)
      primary: "#000000", // Primary color (black)
      onPrimary: "#ffffff", // Text color on primary color (white text on black)
    },
  },
};
